{
  "name": "dastyle",
  "version": "0.0.31",
  "private": true,
  "description": "drafting the style of the universe",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build-core": "lerna exec --scope ds-core -- npm run build",
    "copy-build": "cp packages/ds-core/build/static/js/main.js index.js",
    "build-docs": "lerna exec --scope dastyle-docs -- npm start",
    "docs": "npm run build-core && npm run build-docs",
    "run-local": "npm run build-core && npm run copy-build && npm run build-docs",
    "deploy": "npm run build-core && npm run copy-build && git commit -am 'bump version' && npm version patch && git push origin master && git push --tags"
  },
  "engines": {
    "node": ">= 6.5"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DaPulse/dastyle.git"
  },
  "author": "dapulse Labs Ltd.",
  "bugs": {
    "url": "https://github.com/DaPulse/dastyle/issues"
  },
  "homepage": "https://github.com/DaPulse/dastyle#readme",
  "devDependencies": {
    "eslint": "^3.12.2",
    "eslint-config-airbnb": "^13.0.0",
    "eslint-plugin-import": "^2.2.0",
    "eslint-plugin-jsx-a11y": "^2.2.3",
    "eslint-plugin-react": "^6.8.0"
  }
}
